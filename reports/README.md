# Reports

Daily output lives in `reports/daily/YYYY-MM-DD/`:

- `<agent-id>.md` — each active agent's report (schema in `core/report-schema.md`).
- `brief.md` — the single consolidated HUMAN brief, also pushed to Telegram.

These are committed automatically by the Chief of Staff as the team's audit trail.
